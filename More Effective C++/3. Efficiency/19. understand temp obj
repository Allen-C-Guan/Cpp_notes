/*
 * ******************************** 了解临时对象的来源 **********************************
 * C++中，所有临时对象的产生都不会再你的源代码中出现！临时对象通常会在两个位置产生
 * 1. 隐式类型转换被实行，以求函数调用能够成功
 * 2. 函数返回对象的时候 （如果不用move，而且编译器没有优化的话，就一定会产生临时对象）
 *
 * 1. 隐式转换产生临时变量
 * C++编译器只允许pass by value 和 referenc to const的时候才会产生临时变量，而当pass by reference non-const的时候
 * 并不会发生此类转换！
 * 这是因为当程序员以pass by reference non-const作为入参的时候，就是希望函数可以对其进行更改，而如果产生了临时对象，则不可以
 * 对对象进行修改了，因此编译器禁止了这个行为！
 *
 */
